# Table of contents

The final table of contents is generated from `manifest.json` and the approved prompt
files. It will include clickable chapter entries, PDF bookmarks and page numbers. This
placeholder must never be treated as the final generated contents page.

